# ============================================================
# Root backend requirements
# Covers both modules. Install into a single shared venv.
# ============================================================

# AWS SDK
boto3>=1.39.0
botocore>=1.39.0

# Lambda Powertools
aws-lambda-powertools>=3.0.0

# Data validation
pydantic>=2.8.0

# HTTP (used in seat-purchase for cross-module token validation)
aiohttp>=3.9.0

# ── Testing ──────────────────────────────────────────────────
pytest>=8.2.0
pytest-cov>=5.0.0
moto[dynamodb,events]>=5.0.0

# ── Code quality ─────────────────────────────────────────────
black>=24.4.0
flake8>=7.0.0
mypy>=1.10.0
isort>=5.13.0
pre-commit>=3.7.0
